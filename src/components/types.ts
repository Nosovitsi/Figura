export interface User {
    name: string;
    deals: string[];
}

export interface Organization {
    id: string; 
    name: string;
    organization_name: string;
    address: string;
    phone: string;
    email: string;
  }
  
  